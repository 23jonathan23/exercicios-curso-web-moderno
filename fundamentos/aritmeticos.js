const [a, b, c, d] = [1, 2, 3, 4]

const soma = a + b
const subtracao = a - d
const multiplicacao = b * c
const divisao = c / a
const modulo = a % 2 // resto da divisão // resto 0 = numero PAR, resto 1 = numero IMPAR, claro quando dividido por 2

console.log(soma, subtracao, multiplicacao, divisao, modulo)